# VMC ARM 内部软件开发需求（最简 Use Story）

基于客户文档：SGMW_VMC ARM_防侧翻功能规范_master

## 1. Use Story

作为 VMC ARM 软件，
我需要在车辆存在侧翻风险、且未被更高优先级功能抢占时，
基于车辆动态、风险指标和执行器可用性，完成 ARM 的激活、退出和执行器协调控制，
以便在制动 ARM 深度介入前或介入过程中提升车辆侧倾稳定裕度、降低侧翻风险。

## 2. 功能边界

- VMC ARM 不负责制动 ARM 内部算法，也不负责制动力/驱动力分配。
- VMC ARM 不负责 RWS、CDC、ECAS、主动悬架各自内部闭环控制，只负责请求计算、输出和状态监控。
- VMC ARM 可协调的执行器按车型 Variant 配置，范围包括：RWS、CDC、ECAS、主动悬架。
- 至少 1 个 ARM 相关执行器可用时，功能才允许进入 Active。
- RWS 仅在制动侧可提供准确横摆力矩大小和方向信息时允许参与 ARM 控制。
- HMI、法规、执行器内部 safety 细节不在本 Use Story 范围内。

## 3. 软件需求

### 3.1 状态管理

- 软件应输出 `VMCArmSts`：`0 = Inactive`，`1 = Active`。
- 满足激活条件时，软件进入 Active。
- 满足任一退出条件时，软件退出到 Inactive，并释放所有 ARM 控制请求。

### 3.2 激活条件

软件仅在以下条件同时满足时允许进入 Active：

- ARM 安全监控无故障降级，关键输入信号有效。
- 无高优先级功能激活，例如：坦克掉头、圆规掉头、漂移、制动转向备份功能。
- 至少 1 个 ARM 相关执行器可用。
- 车辆状态满足 ARM 激活判定：
  - `Ax > Cal` 或 `VehSpd > Cal`
  - `Ay > Cal` 或 `StrWhAng > Cal` 或 `StrWhAngRate > Cal`
  - `YawRate > Cal`
  - `ROI > Cal`

### 3.3 退出条件

出现以下任一情况时，软件应退出 Active：

- ARM 安全监控故障降级或关键信号失效。
- 高优先级功能激活。
- 所有 ARM 相关执行器均不可用。
- 车辆状态满足 ARM 退出判定：
  - `Ax < Cal`
  - `VehSpd < Cal`
  - `Ay < Cal`
  - `StrWhAng < Cal`
  - `YawRate < Cal`
  - `ROI < Cal`

### 3.4 输入监控

软件应周期监控以下输入：

- 车辆动态：`Ax`、`Ay`、`VehSpd`、`StrWhAng`、`StrWhAngRate`、`YawRate`、`ROI`
- 功能仲裁：高优先级功能状态
- 安全状态：ARM 相关故障、关键信号有效性
- 执行器状态：可用性、可控性、握手反馈
- 制动侧信息：横摆力矩大小/方向相关可用性信息，用于 RWS 参与判定

### 3.5 控制行为

当 ARM 处于 Active 时，软件应仅对当前可用执行器输出控制请求：

- RWS：输出目标后轮转角、请求标志和有效标志。
- CDC：输出阻尼电流上下限请求，不替代 CDC 自身高频控制，只限制其允许范围。
- ECAS：输出四轮高度请求和刚度请求；当 ECAS 接受 VMC 请求后，VMC 独占目标高度/刚度控制。
- 主动悬架：输出 offset 或 override 模式及四轮目标阻尼力；override 模式下 VMC 对主动悬架拥有更高控制优先级。

### 3.6 仲裁与降级

- 若执行器返回 `Not controllable`、握手失败或执行器故障，软件不得继续对该执行器输出有效 ARM 控制请求。
- 若因此导致无可用执行器剩余，软件应退出 ARM Active。
- ADAS 侧风抗扰等更高优先级功能可抢占相关悬架执行器；被抢占后，软件应按执行器可用性重新仲裁或退出。
- 软件应与 Fault Management 对接，由统一故障管理触发 ARM 降级/退出。

## 4. 对开发同事的最小交付要求

- 实现 ARM 状态机：Inactive / Active。
- 实现激活/退出判定逻辑，阈值可标定。
- 实现执行器可用性判断与独立屏蔽。
- 实现对 RWS、CDC、ECAS、主动悬架的请求封装与发送。
- 实现执行器反馈/握手监控。
- 实现高优先级功能仲裁和退出释放逻辑。
- 实现故障降级后禁止激活、激活中立即退出、退出后请求清零/失效。
- 实现 Variant 配置能力，不同车型可裁剪执行器集合。

## 5. 验收口径

- 满足激活条件时，`VMCArmSts` 可稳定进入 Active。
- 任一退出条件出现时，`VMCArmSts` 应立即回到 Inactive。
- 不可用、故障或被抢占的执行器不得继续被控制。
- ARM 退出后，所有 ARM 请求必须释放。
- 软件支持不同 Variant 的执行器配置裁剪。
- 所有阈值和执行器开关应支持标定或配置。

## 6. 待补充项

- 各激活/退出阈值 `Cal` 的具体数值。
- `ROI` 的明确定义和计算来源。
- 各 Variant 对应的执行器配置矩阵。
- RWS 参与 ARM 时，制动侧“横摆力矩大小/方向准确可用”的判定信号和判据。
- 验收工况与量化性能指标。
